# redis数据库

redis数据库是nosql数据库的代表之一。nosql数据库是指(not only sql)数据库,数据写在内存中，需要配合持久化的操作才能持久保存数据

redis数据库适合高并发，大量写入，高性能或者是键值灵活的场景。和其他nosql数据库相比，redis的特点是单线程，利用io多路复用技术提高效率。

redis里面共有16个数据库，编号从0开始。可以使用`select 编号`命令切换数据库，默认是0号数据库。