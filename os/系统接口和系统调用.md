操作系统提供的接口简单来说就是各种系统函数，像是fork()函数，pthread_create()函数等

CPU的状态分为用户态和内核态，当处于用户态时(CPL==3),不能去访问内核中的数据(DPL==0),因此需要有一个“门”来进行系统调用。

中断是暂时打断CPU执行，开始进行其他的操作。

当程序中调用一个系统函数时，glibc会将其翻译，重点是添加了**0x80**这个中断信号，能让CPU切换状态，以及一个调用系统函数编号eax。CPU通过查询IDT中断信号表来知道要进行系统调用，通过eax里面的值直接跳转到相应的系统函数执行。执行结束后CPU在此切换回用户态。

