当电脑通电开机时，内存上有一段固化的程序，先检查硬件情况，读取引导扇区的boost汇编代码。之后boost执行读取后面的setup和system部分内容，并在最后将指针指向setup。

setup获取各种参数，实模式切换为保护模式。切换为32位机。初步初始化表

下一步是head，解压内核。把镜像搬到正确的地方。把表设置好

main函数部分，就是开始各种硬件初始化。像初始化内存，键盘等


最后在内存中的布局是，操作系统在最下面，上面有gdt和idt两种数据结构表。应用软件在内存的上端运行，这样操作系统就立起来了。